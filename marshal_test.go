package binarystruct

//"bytes"

/*
func TestWrite(t *testing.T) {
	var err error

	s := struct {
		N1 int `binary:"uint32"`
		N2 int `binary:"int32"`
	} { 1, 2 }

	fmt.Println(s)

	var buf bytes.Buffer
	err = Write(&buf, LittleEndian, &s)

	if err!=nil {
		t.Error(err)
		// t.Errorf("%s", err)
		// t.Fatal(err)
		// t.Fatalf("%s", err)
	}
}
*/
